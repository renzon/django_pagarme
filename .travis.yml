language: python
dist: xenial
sudo: true

python:
  - 3.7

end:
  global:
    - PIPENV_VENV_IN_PROJECT=1
    - PIPENV_IGNORE_VIRTUALENVS=1

install:
  - pip install -q pipenv
  - pipenv sync --dev

script:
  - pipenv run flake8 .
